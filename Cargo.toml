[package]
name = "tfhe_comparison"
version = "0.1.0"
edition = "2025"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = "2"
napi-derive = "2"
#tfhe = { version = "~1.0.0", features = ["integer", "gpu" ] }
tfhe = { version = "*", features = ["integer"] }

[build-dependencies]
napi-build = "1"
